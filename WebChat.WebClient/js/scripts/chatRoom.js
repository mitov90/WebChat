(function () {
    'use strict';

    define(['appSettings', 'jquery', 'PubNub'], function (appSettings, $, pubnub) {
        var chatRoom = (function () {

        }());

        return chatRoom;
    });
}());